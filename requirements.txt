# CrisisSync — Python dependencies
# Vercel uses @vercel/python which installs from this file automatically.

# Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Async SQLAlchemy
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0           # SQLite async driver (dev/demo)
# asyncpg==0.30.0             # PostgreSQL async driver (production)

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Validation / settings
pydantic==2.10.3
pydantic-settings==2.7.0
pydantic[email]==2.10.3
email-validator==2.2.0

# Logging
loguru==0.7.3

# HTTP
httpx==0.28.1

# SMS (optional — comment out if not using Twilio)
twilio==9.4.0

# ASGI
python-multipart==0.0.20
